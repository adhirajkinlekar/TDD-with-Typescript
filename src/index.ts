// StringCalculator.ts

export class StringCalculator {

    public add(numbers: string): number {
        
        if (numbers === "") {
            return 0;
          }

        return 0;
    }
  }
  